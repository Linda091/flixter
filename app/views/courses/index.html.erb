<br/>

<div class= "booyah-box col-10 offset-1 text-center">
  <h1 class= "text-center">Learn How To Code Today</h1>
<hr/>

  <% @courses.each do |course| %>
    <h2><%=link_to course.title, course_path(course), class: 'text-center' %></h2>
    <br/>
    <%= image_tag course.image, class: 'img-fluid' %>
    <br/><br/>
    <div class="row">
      <div class="col-sm-8 col-12 text-center">
        <%= course.description %>
      </div>
      <br/><br/>
      <div class="col-sm-4 col-12 text-center">
        <%= link_to 'More Details', course_path(course), class: 'btn btn-primary btn-large' %>
      </div>
    </div>
    <br class="clr" />
  <% end %>
</div>
<br/><br/>